{
  "name": "shreyam-website",
  "private": true,
  "description": "End-to-end tests for the Hugo site. The site itself needs no Node toolchain — this package exists only for Playwright.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  },
  "engines": {
    "node": ">=20"
  }
}
